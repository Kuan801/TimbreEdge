# OLED 選單完整說明

四顆按鈕：**上 = D2、下 = D3、確定 = D4、返回 = D5**。
插了 USB 電腦鍵盤的話，**上下方向鍵 = 上下，Enter = 確定，Esc/Backspace = 返回**。

---

## 先看懂這四頁在幹嘛

整個流程是一條產線，四頁剛好是四個階段，**順序不能跳**：

```
Sampling  ──►  Timbre  ──►  Training  ──►  Play
（取素材）    （變音色）    （選用加強）   （聽成果）

 錄下真實         把 WAV 拆成          用 MLP 學      用合成器
 樂器的單音       音色指紋             音高之間        彈出來
 存成 WAV        （諧波、包絡…）       的變化
```

最常見的困惑是 **Sampling 和 Timbre 的差別**：

- **Sampling** 只負責「把聲音錄成 WAV 檔」，錄完什麼事都還沒發生。
- **Timbre** 才是把 WAV 讀進來、算出音色指紋、存進音色庫。**沒做這一步，Play 不會有聲音。**

第二個困惑是 **Training 是選用的**。不訓練也能彈——那叫「關鍵影格模式」，是預設的
運作方式。訓練 MLP 只是讓音高之間的變化更平滑，不是必要條件。

---

## 主選單（TimbreClone）

| 項目 | 作用 |
|---|---|
| **Play** | 進入演奏頁 |
| **Sampling** | 進入採樣頁 |
| **Timbre** | 進入音色頁 |
| **Training** | 進入訓練頁 |
| **Status** | 讓選單退開，露出底下的狀態面板（按**返回**回到選單） |

> **Status 不是一個動作**，它只是把畫面讓給狀態面板。面板上會顯示 ADSR、諧波數、
> 訓練集大小、USB 鍵盤有沒有連上——那些資訊平常被選單蓋住了。

---

## Play — 演奏

| 項目 | 序列埠指令 | 作用 | 注意 |
|---|---|---|---|
| **Keyboard 12 keys** | `?keys` | 進入**實體琴鍵模式**：接在 24~30、34~38 的 12 顆按鈕變成 C4~B4 | 進去之後只有**返回鍵**有作用（離開）。上下鍵故意停用，避免演奏中誤觸移調 |
| **PC keyboard map** | `?pckb` | 顯示 **USB 電腦鍵盤**的鍵位對照，序列埠會印完整版 | 只是說明畫面，不用進什麼模式——電腦鍵盤插上去隨時都能彈 |
| **Scale** | `p` | 演奏半音階，一個半音一個單音 | 音域是**音色庫實際涵蓋的範圍再往上一個八度**，不是寫死的。**沒有音色會直接拒絕**並在序列埠說明 |
| **Scale + record** | `w` | 同上，但**同時錄成 `PLAY.WAV`** 存到 SD | 要拿去電腦上跑 `tools/evaluate.py` 就用這個 |
| **Canon** | `j n` | 演奏**卡農**：D 大調三聲部，約 45 秒 | 拿來聽的，不是拿來量的。音高會依音色庫逐聲部做八度安置 |
| **Canon + record** | `j` | 同上，但**同時錄成 `CANON.WAV`** | 跟 `PLAY.WAV` 分開存，不會蓋掉評測要用的那份 |
| **Stop** | `x` | 停止演奏，回到閒置畫面 | |
| **Key octave** | `?oct %d` | 琴鍵的移調，−2 ~ +2 個八度 | **數值項**：確定進入編輯（顯示 `[0]`）→ 上下改值 → 再按確定或返回才生效。實體琴鍵和電腦鍵盤**共用**這個設定 |
| **Volume +** / **−** | `+` `-` | 主音量，每次 0.05，上限 0.85 | |

### 兩份譜的分工

**半音階**不是拿來聽的，是拿來量的：每個音獨立、不重疊，`tools/evaluate.py`
才能自動切開並跟對應的素材逐音比對。所有評測數字都是從它來的。

**卡農**剛好相反 —— 三聲部同時發聲，逐音比對切不開，它的用途就是耳朵：
LSD 與包絡相關性再漂亮，也回答不了「拿來彈一首曲子像不像」。三輪分別是
二分音符、四分音符、八分音符琶音，起音與釋放的品質在第三輪最聽得出來。

卡農的音高不是寫死的：原譜低音到 D3、旋律到 F#5，跨了 29 個半音，而音色庫
只有一個八度時可用的範圍只有 24 個 —— 所以會**逐聲部各自選一個八度**塞進去
（只移 12 的倍數，調性不變）。實際移了多少會印在序列埠上。

---

## Sampling — 用麥克風取素材

> 這一頁**只產生 WAV 檔**。錄完還要去 Timbre 頁才會變成音色。
> 已經有現成 WAV 檔（放在 SD 上）的話，整頁可以跳過。

| 項目 | 指令 | 作用 | 注意 |
|---|---|---|---|
| **Record 1 note** | `r` | 倒數 0.6 秒後錄 2 秒 → `REC.WAV`，**錄完自動分析並給判定** | 錄一個「持續的單音」。分析完會顯示這次錄音行不行（見下面「錄完的判定面板」）|
| **Auto sampling** | `s` | **連續採樣模式**：偵測到聲音就自動錄、分析、依音高命名存檔、入庫 | 一個人拿樂器沒辦法同時按鍵盤，所以有這個模式。彈 C4 就存成 `C4.WAV`。按**任意鍵**結束。開頭會先聽 600 ms 量環境噪音（顯示 `listening to room...`），門檻是自動的 |
| **Input monitor** | `o` | 顯示輸入電平 | **麥克風沒反應時先按這個。** 數字完全不動 = 麥克風沒接好，或輸入來源選錯 |
| **Mic gain** | `g %d` | 麥克風增益 0~63 dB | 數值項。目標是讓錄音峰值落在 **0.3~0.8**。太小 = 雜訊比例高，太大 = 削波 |
| **Mic / Line in** | `i` | 切換麥克風 / 線性輸入 | 用 audio shield 上的 MIC 焊盤選前者，3.5mm LINE IN 選後者 |

### 錄完的判定面板

`Record 1 note` 與 `Auto sampling` 的每一個音，分析完都會顯示：

```
        REC OK
  C4 262Hz  1.80s        ← 音名 / 基頻 / 有效音長
  pk0.45 snr48 dec0.55   ← 峰值 / 訊噪比 dB / 每秒衰減倍率
  levels look good       ← 結論
  added to bank          ← 下一步
```

**判定不合格（標題變成 `REC FAILED`）時面板會停著等你按鍵，而且不會自動演奏**
—— 那會蓋掉判定，而判定正是重點。合格的話顯示 4 秒後才自動演奏。

| 顯示 | 意思 | 怎麼改 |
|---|---|---|
| `N notes in one take` | 一次錄到不只一個音 | 只彈一次，讓它自然響完 |
| `clipped x%` | 削波，波形被削平 | `Mic gain` 調低 |
| `too quiet: peak x` | 電平低而且埋在噪音裡 | `Mic gain` 調高，或靠近麥克風 |
| `noisy: SNR x dB` | 觸發被環境噪音搶先 | 換安靜的環境 |
| `note too short` | 包絡量不出衰減走勢 | 音拉長到 1~2 秒 |
| `no decay measured` | 幾乎不衰減 | 管風琴／長弓弦樂正常，撥弦樂器就有問題 |

採樣模式的等待畫面會一直顯示 `amb0.021 thr0.045`：左邊是量到的環境噪音，
右邊是**自動算出來的門檻**（= 環境噪音 × 1.5，但不低於你設的下限）。
`room too noisy` 代表環境噪音已經超過你設的下限，`low margin - play up`
代表上一個音只比環境大一點點，錄起來會很勉強。

判定 `DROPPED` 的那一筆是**整筆丟掉**：不存檔、不入庫、不進訓練集。

`snr--` 代表訊噪比**量不到**，不是很好：連續採樣有 93 ms 前置緩衝，音一觸發
就從緩衝最前面開始寫檔，起音落在第 0 格，前面沒有可以當底噪的那一段。
那時候所有跟訊噪比有關的規則都會跳過。

### 連續採樣的三個設計

- **前置緩衝 93 ms**：等電平超過門檻時起音已經發生了，直接開錄會切掉最關鍵的
  起音瞬態——而起音正是人耳辨識樂器最主要的線索。所以永遠保留觸發前 32 個 block。
- **自動命名**：分析完才知道音高，所以先錄到 REC.WAV，算出 f0 之後才複製成 `C4.WAV`。
- **自動入庫**：存檔的同時就加進音色庫，不用再去 Timbre 頁。

---

## Timbre — 把素材變成音色

**這一頁是整條產線的核心。跳過它，Play 一定沒聲音。**

| 項目 | 指令 | 作用 | 什麼時候用 |
|---|---|---|---|
| **Load all WAV** | `n *` | 掃描 SD **根目錄**所有 WAV，逐一分析並建立**音色庫** | 素材已經在 SD 上時的標準做法。**只掃根目錄** —— 連續採樣的音檔在 `SETnn/` 裡，要用下一項 |
| **Load newest SET** | `n SET` | 載入最近一輪連續採樣的 `SETnn/` 資料夾 | 「最近」= 這次開機採過的話就是那一組，否則編號最大的那組 |
| **Analyze REC** | `a` | 只分析 `REC.WAV` 這**一個**檔 | 剛按過 Record 1 note、想重跑分析時 |
| **Bank coverage** | `v` | 顯示音色庫涵蓋哪些音高、共幾組 | **合成聽起來怪就先看這個。** 音色庫只有 C4，卻要彈 C6，那就是移調兩個八度，不像很正常 |
| **Reload profile** | `l` | 從 SD 重新讀 `PROFILE.BIN` | 換過 SD 卡、或想回到上次存的音色 |
| **Clear trainset** | `z` | **清空訓練集和音色庫** | 換樂器時用。一次只能複製一個音色，換樂器就得重來 |
| **Delete WAV files** | 子頁 → `y y` | 刪掉**根目錄**程式產生的音檔（REC / PLAY / 音名檔） | 卡快滿了、或想從乾淨狀態重來 |
| **Delete SET dirs** | 子頁 → `y s` | 刪掉**全部**採樣資料夾 `SETnn` | 同上，但針對每一輪採樣存下來的那幾組 |

兩個刪除都是**獨立一頁做確認**，而且 **Cancel 放第一項**——進去游標停在第 0 項，
手滑連按兩下確定只會取消，不會刪東西。刪完會顯示刪了幾個檔、釋放多少空間。

音色資料（`BANK.BIN` / `PROFILE.BIN` / `MODEL.BIN`）**不會**被這兩個刪掉，
要清音色請用 `Clear trainset`。使用者自己放的素材（`Piano.mf.C4.wav` 這種有前綴的）
也不在刪除範圍內。

只想刪某一組的話，選單沒有這個功能，序列埠打 `y s SET02`。

### 每一輪採樣一個資料夾

按 `Auto sampling` 會自動建立 `SET01/`、`SET02/`…，那一輪的音檔連同
`BANK.BIN` 都收在裡面：

```
SET01/  C4.WAV  D4.WAV  E4.WAV …  BANK.BIN
```

以前全部堆在根目錄，第二輪的 `C4.WAV` 會直接蓋掉第一輪的，而且
`Load all WAV` 會把不同樂器、不同輪次的素材混在一起訓練，從外面看不出來。

要載入某一組：選單的 **Load newest SET**（最近那一組），或序列埠 `n SET01/`
指定任何一組。想換回某一輪的音色而不重跑分析，把那個資料夾裡的 `BANK.BIN`
複製回根目錄就好。

**注意 `Load all WAV` 看不到這些音檔。** 它只掃根目錄，而採樣的音檔在
`SETnn/` 裡；根目錄剩下的只有 `REC.WAV`（每錄一個音就被覆寫的暫存檔），
所以採了十幾個音之後按它會顯示「只跑了一個」。多數時候其實不用重載 ——
採樣時每個音就已經進音色庫並存了 `BANK.BIN`，先按 `Bank coverage` 確認。

### Load all WAV vs Analyze REC

差別是**一批** vs **一個**，以及**進不進音色庫**：

- `Analyze REC` 產生的是**單一 profile**——一個音高的音色指紋。
- `Load all WAV` 產生的是**音色庫**（最多 16 組）。彈某個音時，系統會挑**音高最接近**
  的那一組來用，所以素材涵蓋的音域越廣，移調距離越短，越像。

小號那次的實測：只用 C4~B4 訓練，往上移 4~12 個半音時對數頻譜距離是 2.6 dB，
在音域內只有 1.2 dB。**移調距離直接決定像不像。**

---

## Training — 選用的 MLP 加強

> 不做這一頁也能正常演奏。預設的「關鍵影格內插」已經可用。

| 項目 | 指令 | 作用 | 注意 |
|---|---|---|---|
| **Epochs** | `t %d` | 訓練回合數，50~2000，每步 50 | ⚠️ **這是數值項，但離開編輯就會立刻開始訓練**，訓練完還會自動演奏並錄成 PLAY.WAV。不是只改個設定 |
| **MLP on/off** | `k` | 在「MLP」和「純關鍵影格」之間切換 | **A/B 比較用。** 想知道訓練有沒有幫上忙，就切過去聽一次 |
| **Reload MODEL** | `m` | 從 SD 重新讀 `MODEL.BIN` | 在電腦上用 `tools/train_ddsp.py` 訓練完，把檔案拷回 SD 之後用 |

### 訓練前要先有素材

訓練集是靠 Timbre 頁的 `Load all WAV` 累積的。訓練集空的時候訓練沒有意義——
狀態面板上的 `train N smp / M pit` 就是告訴你目前有幾筆樣本、涵蓋幾個音高。
**M 至少要 3 以上**才值得訓練，不然 MLP 學不到音高之間的變化規律。

---

## 第一次上手的最短路徑

**情況 A：SD 上已經有單音 WAV 檔**

```
Timbre → Load all WAV          等它跑完（素材在根目錄時用這個；
                               剛用 Auto sampling 採完的話改用 Load newest SET）
Timbre → Bank coverage         確認音域涵蓋範圍
Play   → Scale                 逐音聽（要量的話用 Scale + record）
Play   → Canon                 聽它彈一段曲子
```

**情況 B：要用麥克風現場錄**

```
Sampling → Input monitor       確認麥克風真的有訊號（數字會動）
Sampling → Mic gain            調到峰值 0.3~0.8
Sampling → Auto sampling       一個半音一個半音彈上去，音與音之間停半秒
                               （錄音、分析、命名、入庫全自動）
任意鍵                          結束
Play     → Scale               逐音聽
Play     → Canon               聽它彈一段曲子
```

想加強的話，最後再：`Training → Epochs`（調到 300 左右按返回）。

---

## 幾個容易踩的地方

| 現象 | 原因 | 怎麼辦 |
|---|---|---|
| 按 Scale 沒反應 | 還沒有音色 | 先做 Timbre 頁。狀態面板會顯示 `no timbre loaded` |
| 數值改了沒作用 | 數值項**離開編輯**才送出，不是每按一下就送 | 改完要再按確定或返回 |
| 調完 Epochs 突然開始跑 | 這是設計如此——離開編輯就開始訓練 | 只想看目前值的話，進編輯後直接按返回 |
| 琴鍵模式出不來 | 進去之後只有返回鍵有作用 | 按返回。上下鍵是故意停用的 |
| 合成聽起來不像 | 多半是移調距離太遠 | `Bank coverage` 看音域，補錄幾個音高 |
| 麥克風完全沒聲音 | 來源選錯或沒焊好 | `Input monitor` 看數字動不動，再用 `Mic / Line in` 切換 |
| 麥克風有聲音但很小 | `micGain` 預設 36，上限是 62.5 dB | `Input gain` 往上調（或序列埠 `g 63`），峰值抓 0.3~0.8 |
| 採樣錄了十幾個音，`Load all WAV` 只跑一個 | 採樣存在 `SETnn/`，`Load all WAV` 只掃根目錄 | 用 `Load newest SET`。不過通常不用重載，先按 `Bank coverage` 看 |
| 選單被狀態畫面蓋住 | 演奏/分析時選單會讓位 | 按返回叫回選單 |

---

## 畫面上的其他資訊

閒置時狀態面板四行分別是：

```
A0.02 D0.35 S0.60      ← ADSR：起音/衰減/延持
harm 32  noise 0.08    ← 諧波數、噪聲比例
train 12 smp / 5 pit   ← 訓練集：12 筆樣本、涵蓋 5 個音高
PC kbd ready  oct+0    ← USB 鍵盤狀態與目前八度
```

沒插鍵盤時第四行會換成 `USB: no keyboard`。
右上角捲動時會顯示「第幾項 / 共幾項」。
